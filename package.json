{
  "name": "dockerimages",
  "version": "0.0.1",
  "description": "This is an all in one project.",
  "main": "index.js",
  "scripts": {
    "build": "yarn build:worker && yarn build:worker && yarn build:service",
    "build:proxy": "cd ./proxy && sh dockerbuild.sh",
    "build:worker": "cd ./worker && yarn install && sh dockerbuild.sh",
    "build:service": "cd ./service && yarn install && yarn build && sh dockerbuild.sh",
    "start": "yarn start:database && yarn start:service && yarn start:proxy",
    "start:database": "cd ./database && sh dockerrun.sh",
    "start:workder": "cd ./workder && sh dockerrun.sh",
    "start:service": "cd ./service && sh dockerrun.sh",
    "start:proxy": "cd ./proxy && sh dockerrun.sh"
  },
  "repository": {
    "type": "git"
  },
  "author": "Chris Wei",
  "devDependencies": {
  }
}
